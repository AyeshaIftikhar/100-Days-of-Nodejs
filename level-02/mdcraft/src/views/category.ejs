<h1>Category: <%= name %></h1>
<ul class="list">
  <% posts.forEach(p => { %>
    <li><a href="<%= p.url %>"><%= p.title %></a> — <span class="meta"><%= new Date(p.date).toDateString() %></span></li>
  <% }) %>
</ul>
