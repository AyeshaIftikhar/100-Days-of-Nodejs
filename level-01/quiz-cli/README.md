# Quiz CLI App (Readline)

A command-line quiz application using Node.js's built-in readline module for interactive input.

## Features

- Multiple question types (multiple choice, true/false, short answer)
- Score tracking
- Timed questions
- Colorful output
- Configurable quizzes via JSON
  -Question randomization
