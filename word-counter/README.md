# Word Counter
A comprehensive word counter implementation that uses Node.js's file system (fs) module to count words in files.

## Features
- Count words in a file
- Count words in all files in a directory
- Support for multiple file types (.txt, .md, .js, etc.)
- Line count and character count
- Options to exclude common words (stop words)
- Recursive directory scanning
- Progress indicators

