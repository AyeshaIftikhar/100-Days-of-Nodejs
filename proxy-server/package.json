{
  "name": "proxy-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Ayesha Iftikhar @ Aishayy",
  "license": "ISC",
  "description": "An HTTP proxy server acts as an intermediary between clients and servers, forwarding client requests to other servers and returning responses back to the clients. This guide will walk you through creating a basic HTTP proxy server in Node.js with explanations at each step.",
  "dependencies": {
    "express": "^5.1.0",
    "http-proxy": "^1.18.1",
    "http-proxy-middleware": "^3.0.5",
    "morgan": "^1.10.0"
  }
}
