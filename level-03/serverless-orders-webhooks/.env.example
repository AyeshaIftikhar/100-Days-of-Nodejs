# Core
AWS_REGION=us-east-1
STAGE=dev
TABLE_NAME=OrdersTable-dev

# When running locally against DynamoDB Local
DYNAMO_ENDPOINT=http://localhost:8000

# Shared secret for webhook HMAC verification (hex/base64/utf8 secret)
WEBHOOK_SECRET=change-me
